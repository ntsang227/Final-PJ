<!DOCTYPE html>
<html>
<head>
  <title>Danh sách yêu cầu đăng ký</title>
</head>
<body>
  <h1>Thông tin đăng ký:</h1>
  <% for (let i = 0; i < course.length; i++) { %>
    <% if (course[i].nameuser) { %>
      <div>
        <p>Tên khóa học: <%= course[i].name %></p>
        <p>Tên gia sư: <%= course[i].nametutor %></p>
        <p>Tên người dùng: <%= course[i].nameuser %></p>
        <p>Ngày đăng ký: <%= course[i].datePost %></p>
        <form method="post" action="/tutor/accept">
          <input type="hidden" name="courseId" value="<%= course[i]._id %>">
          <button class="btn">Đồng ý</button>
        </form>
        <form method="post" action="/tutor/ancel">
          <input type="hidden" name="courseId" value="<%= course[i]._id %>">
          <button class="btn-x">Xóa</button>
        </form>
      </div>
    <% } %>
  <% } %>
  
</body>
</html>